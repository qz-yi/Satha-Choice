# 🚀 دليل تشغيل مشروع Satha Choice

## ✅ التعديلات التي تمت

1. **إصلاح NODE_ENV لـ Windows**: تم استخدام `cross-env` في `package.json`
2. **إضافة دعم متغيرات البيئة**: تم تثبيت وإعداد `dotenv`
3. **إصلاح Request Flow**: تم تحديث منطق التواصل بين الزبون والسائق

## 📋 المتطلبات الأساسية

### 1. تثبيت PostgreSQL

يجب تشغيل قاعدة بيانات PostgreSQL قبل تشغيل المشروع.

**الطريقة الأسهل - باستخدام Docker:**
```bash
docker run --name satha-postgres -e POSTGRES_PASSWORD=password -e POSTGRES_DB=satha_choice -p 5432:5432 -d postgres
```

**أو تثبيت PostgreSQL مباشرة:**
- تحميل من: https://www.postgresql.org/download/windows/
- أنشئ قاعدة بيانات باسم `satha_choice`

### 2. تحديث ملف .env

عدّل ملف `.env` ليطابق إعدادات قاعدة البيانات لديك:

```env
DATABASE_URL=postgresql://postgres:password@localhost:5432/satha_choice
PORT=3000
```

## 🔧 خطوات التشغيل

### 1. تثبيت الحزم
```bash
npm install
```

### 2. إنشاء جداول قاعدة البيانات
```bash
npm run db:push
```

### 3. تشغيل المشروع
```bash
npm run dev
```

## 🌐 الوصول للتطبيق

بعد التشغيل الناجح:
- **Frontend**: http://localhost:3000
- **API**: http://localhost:3000/api

## 🆘 حل المشاكل الشائعة

### خطأ: ECONNREFUSED :5432
❌ **المشكلة**: PostgreSQL غير شغال  
✅ **الحل**: شغل PostgreSQL أو Docker container

### خطأ: Port already in use
❌ **المشكلة**: المنفذ مستخدم  
✅ **الحل**: غيّر `PORT` في ملف `.env`

### خطأ: DATABASE_URL must be set
❌ **المشكلة**: ملف `.env` مفقود أو خطأ  
✅ **الحل**: تأكد من وجود ملف `.env` في الجذر

## 📝 ملاحظات مهمة

- ✅ تم إصلاح `cross-env` للعمل على Windows
- ✅ تم إضافة خطوط الملاحة بين الزبون والسائق
- ✅ تم تفعيل الدردشة التلقائية عند قبول الطلب
- ✅ يتم إرجاع الزبون لصفحة Booking عند تأكيد الاستلام

## 🎯 الميزات الجديدة في Request Flow

1. **معلومات السائق الفورية**: يرى الزبون رقم الهاتف ولوحة السيارة فوراً
2. **خط الملاحة**: خط برتقالي يربط السائق بالزبون على الخريطة
3. **معلومات الزبون للسائق**: السائق يرى كل تفاصيل الزبون عند القبول
4. **دردشة تلقائية**: تفعيل تلقائي للدردشة بين الطرفين
5. **إعادة توجيه ذكية**: إرجاع الزبون للصفحة الرئيسية بعد الاستلام
